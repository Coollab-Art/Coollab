// layout(std430, binding = 0) buffer _positions_buffer
// {
//     vec2 _positions[];
// };
#include "_COOL_RES_/shaders/rand.glsl"
uniform uvec2 _grid_size;

layout(std430, binding = 1) buffer _densities_buffer
{
    float _densities[];
};

void cool_main()
{
    uint gid        = gl_GlobalInvocationID.x;
    _densities[gid] = hash_0_to_1_1D_to_1D(gid);
}

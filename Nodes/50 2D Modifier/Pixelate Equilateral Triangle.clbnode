// To learn how to write nodes, see https://coollab-art.com/Tutorials/Writing%20Nodes/Intro

#include "_COOL_RES_/shaders/hexagonal_grid.glsl"
#include "_COOL_RES_/shaders/triangle_grid.glsl"

INPUT float 'Cell Size';

OUTPUT float 'Index X';
OUTPUT float 'Index Y';

UV main(UV uv)
{
    // float grid_size = 'Grid Size';
    // uv *=grid_size * 2.;
    // ivec2 id1, id2;
    // id1.x = int(floor(uv.x * 2.));
    // id2.x = int(ceil(uv.x * 2.));
    // id1.y = int(floor(uv.y * 2. /sqrt(3.)));
    // id2.y = id1.y;
    
    // vec2 center1 = Cool_tri_center_from_tri_id(id1 , grid_size);
    // vec2 center2 = Cool_tri_center_from_tri_id(id2, grid_size);
    // if(length_squared(uv - center1)< .01 || .01 > length_squared(uv - center2))
    // {
    //     return ivec2(-10, -10);
    // }    
    // if(((id1.x+id1.y) / 2) * 2 != id1.x+id1.y)
    // {
    //     center1.y += sqrt(3.)/2.;
    //     center2.y -= sqrt(3.)/2.;
    // }
    // else
    // {
    //     center1.y -= sqrt(3.)/2.;
    //     center2.y += sqrt(3.)/2.;
    // }
    // // if(length_squared(uv - center1)< .01 || .01 > length_squared(uv - center2))
    // // {
    // //     return ivec2(-10, -10);
    // // }
    // center1.y /= sqrt(3.);
    // center2.y /= sqrt(3.);
    // uv.y /= sqrt(3.);

    // ivec2 id;
    // if(length_squared(uv - center1) < length_squared(uv - center2))
    // {
    //     return Cool_tri_center_from_tri_id(id1 , grid_size);
    // }
    // else
    // {
    //     return Cool_tri_center_from_tri_id(id2 , grid_size);
    // }

    ivec2 id = id_test(uv, 'Cell Size' * 2.);
    'Index X' = id.x;
    'Index Y' = id.y;
    return(Cool_tri_center_from_tri_id(id, 'Cell Size' * 2.));






    // vec2  gv = Cool_hex_uv(uv, 'Grid Size');
    // ivec2 id = Cool_hex_id(uv, 'Grid Size');

    // float angle = Cool_angle(gv);

    // float id_triangle = (angle * 360 / (2 * PI) > 330.) ? 0. : floor((angle * 360 / (2 * PI) + 30.) / 60);

    // 'Index X' = id.x * 6 + id_triangle;
    // 'Index Y' = id.y * 6 + id_triangle;

    // float angle_middle = (id_triangle * 60. / 360.) * 2 * PI;
    // //
    // vec2 v = vec2((2. / 6.) * cos(angle_middle), (2. / 6.) * sin(angle_middle)) / 'Grid Size';

    // vec2 center = Cool_hex_center_from_hex_id(id, 'Grid Size');

    // return center + v;
}
